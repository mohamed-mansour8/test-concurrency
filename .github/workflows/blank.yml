name: Hello World Action

on: 
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print Hello World
        run: |
          echo "Hello, World!"
          sleep 60  # Sleep for 1 minute
